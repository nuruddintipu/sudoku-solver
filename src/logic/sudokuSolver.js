function sudokuSolver(board) {
    // const isValid = (board, row, col, num) => {
    //     for(let i = 0; i < 9; i++){
    //         if(board[row][i] === num || board[i][col] === num || board[Math.floor])
    //     }
    // }
}

export default sudokuSolver;